// Generated by IcedCoffeeScript 1.8.0-c
(function() {
  var conf, couch_utils, db, feed, follow, get_new_synced_state, get_unsynced_audit_entries, iced, opts, org, orgs, resources, team_validation, update_audit_entries, update_audit_entry, utils, wh, _, __iced_k, __iced_k_noop, _i, _len;

  iced = require('iced-runtime');
  __iced_k = __iced_k_noop = function() {};

  _ = require('underscore');

  follow = require('follow');

  couch_utils = require('./couch_utils');

  conf = require('./config');

  team_validation = require('./design_docs/org/lib/validation');

  utils = require('./utils');

  wh = require('./worker_helpers');

  orgs = conf.ORGS;

  resources = {
    gh: require('./workers/gh').handlers
  };

  get_unsynced_audit_entries = function(doc) {
    return _.filter(doc, function(entry) {
      return !entry.synced;
    });
  };

  update_audit_entry = function(old_entry, new_synced_state) {
    if (old_entry.synced) {
      return false;
    }
    if (old_entry.synced === new_synced_state) {
      return false;
    }
    old_entry.synced = new_synced_state;
    return true;
  };

  get_new_synced_state = function(old_entry, new_synced_state) {
    if (old_entry.synced) {
      return old_entry.synced;
    }
  };

  update_audit_entries = function(db, team_id, sync_status, resource_updates, callback) {
    var dirty, entry, entry_id, err, final_synced, new_synced, old_data, old_synced, resource, resp, team, update, updates, ___iced_passed_deferral, __iced_deferrals, __iced_k;
    __iced_k = __iced_k_noop;
    ___iced_passed_deferral = iced.findDeferral(arguments);
    (function(_this) {
      return (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "/Users/greisend/programming/devdash/kratos/src/worker.iced"
        });
        db.get(team_id, __iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              err = arguments[0];
              return team = arguments[1];
            };
          })(),
          lineno: 30
        }));
        __iced_deferrals._fulfill();
      });
    })(this)((function(_this) {
      return function() {
        if (err) {
          return callback(err);
        }
        dirty = false;
        for (entry_id in sync_status) {
          new_synced = sync_status[entry_id];
          entry = _.findWhere(team.audit, {
            id: entry_id
          });
          old_synced = entry.synced;
          final_synced = old_synced || new_synced;
          if (final_synced !== old_synced) {
            dirty = true;
            entry.synced = final_synced;
          }
        }
        for (entry_id in resource_updates) {
          updates = resource_updates[entry_id];
          for (resource in updates) {
            update = updates[resource];
            if (update) {
              dirty = true;
              old_data = utils.mk_objs(team, ['rsrcs', resource, 'data'], {});
              _.extend(old_data, update);
            }
          }
        }
        (function(__iced_k) {
          if (dirty) {
            (function(__iced_k) {
              __iced_deferrals = new iced.Deferrals(__iced_k, {
                parent: ___iced_passed_deferral,
                filename: "/Users/greisend/programming/devdash/kratos/src/worker.iced"
              });
              db.insert(team, __iced_deferrals.defer({
                assign_fn: (function() {
                  return function() {
                    err = arguments[0];
                    return resp = arguments[1];
                  };
                })(),
                lineno: 50
              }));
              __iced_deferrals._fulfill();
            })(function() {
              if ((typeof err !== "undefined" && err !== null ? err.status_code : void 0) === 409) {
                return update_audit_entries(db, team_id, entries, errs, callback);
              } else {
                return callback(err);
              }
              return __iced_k();
            });
          } else {
            return __iced_k();
          }
        })(function() {
          return callback();
        });
      };
    })(this));
  };

  for (_i = 0, _len = orgs.length; _i < _len; _i++) {
    org = orgs[_i];
    db = couch_utils.nano_admin.use('org_' + org);
    opts = {
      db: db.config.url + '/' + db.config.db,
      include_docs: true
    };
    feed = new follow.Feed(opts);
    feed.filter = function(doc, req) {
      if (!team_validation.is_team(doc)) {
        return false;
      } else {
        return true;
      }
    };
    feed.on('change', function(change) {
      var doc, entry, err, errs, handler, handlers, resource, resource_updates, sync_status, unsynced_audit_entries, ___iced_passed_deferral, __iced_deferrals, __iced_k;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      console.log('handling change');
      doc = change.doc;
      unsynced_audit_entries = get_unsynced_audit_entries(doc.audit);
      errs = {};
      resource_updates = {};
      (function(_this) {
        return (function(__iced_k) {
          var _j, _len1;
          __iced_deferrals = new iced.Deferrals(__iced_k, {
            parent: ___iced_passed_deferral,
            filename: "/Users/greisend/programming/devdash/kratos/src/worker.iced"
          });
          for (_j = 0, _len1 = unsynced_audit_entries.length; _j < _len1; _j++) {
            entry = unsynced_audit_entries[_j];
            errs[entry.id] = {};
            resource_updates[entry.id] = {};
            handlers = wh.get_handlers(entry, resources);
            for (resource in handlers) {
              handler = handlers[resource];
              handler(entry, doc, __iced_deferrals.defer({
                assign_fn: (function(__slot_1, __slot_2, __slot_3, __slot_4) {
                  return function() {
                    __slot_1[__slot_2] = arguments[0];
                    return __slot_3[__slot_4] = arguments[1];
                  };
                })(errs[entry.id], resource, resource_updates[entry.id], resource),
                lineno: 84
              }));
            }
          }
          __iced_deferrals._fulfill();
        });
      })(this)((function(_this) {
        return function() {
          sync_status = {};
          _.each(errs, function(rsrc_errs, entry_id) {
            var success;
            success = !Boolean(_.find(rsrc_errs, function(err) {
              return err;
            }));
            if (!success) {
              console.log('ENTRY ERROR:', errs);
            }
            return sync_status[entry_id] = success;
          });
          (function(__iced_k) {
            __iced_deferrals = new iced.Deferrals(__iced_k, {
              parent: ___iced_passed_deferral,
              filename: "/Users/greisend/programming/devdash/kratos/src/worker.iced"
            });
            update_audit_entries(db, doc._id, sync_status, resource_updates, __iced_deferrals.defer({
              assign_fn: (function() {
                return function() {
                  return err = arguments[0];
                };
              })(),
              lineno: 92
            }));
            __iced_deferrals._fulfill();
          })(function() {
            if (err) {
              return console.log(err);
            }
          });
        };
      })(this));
    });
    feed.on('error', function(err) {
      return console.log(err);
    });
    feed.follow();
  }

}).call(this);
